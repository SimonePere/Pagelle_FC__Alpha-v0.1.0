/**
 * 🧪 TEST PAGE — CreateMatch Preview
 *
 * Mostra il prototipo UX migliorato del flusso "Crea Partita" con dati mock.
 */

import React from 'react';
import CreateMatch from '../components/CreateMatch';

const TestVote: React.FC = () => {
    return <CreateMatch />;
};

export default TestVote;
