{
  "name": "pagelle-fc-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prod": "set NODE_ENV=production && node server.js",
    "test:env": "set NODE_ENV=test && nodemon server.js",
    "dev:env": "set NODE_ENV=development && nodemon server.js",
    "seed": "node src/utils/seedData.js",
    "seed:test": "set NODE_ENV=test && node src/utils/seedData.js",
    "db:backup": "set NODE_ENV=test && node migrate-db.js backup",
    "db:restore": "set NODE_ENV=production && node migrate-db.js restore",
    "db:migrate": "set NODE_ENV=test && node migrate-db.js migrate",
    "db:list": "node migrate-db.js list"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-async-handler": "^1.2.0",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.0.0",
    "morgan": "^1.10.1",
    "node-cron": "^4.2.1",
    "validator": "^13.15.23"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
